---
_provenance:
  writtenBy: stardust:uplift
  writtenAt: 2026-06-24T00:00:00Z
  againstInput: https://www.hirslanden.ch/
  readArtifacts:
    - stardust/current/_brand-extraction.json
    - stardust/current/pages/home.json
    - stardust/current/brand-review.html
  note: >
    User override — 5 STATIC variants, no cinematic. Candidates below are evaluated for
    static composition/IA/voice/type/color bets, not for motion amplification. The
    register-driven C-cinematic selection in the standard uplift contract is not used.
---

# What-if candidates — https://www.hirslanden.ch/

From the closed catalog in `reference/what-if-candidates.md`.

1. **Display-typography amplification** — "What if Metropolis carried a confident editorial
   display scale instead of a soft 1.4-line-height stack?" Evidence: single geometric sans
   (400/500/700), currently ad-hoc scale. Serves variant **D** (typographic system bet).
   Not disqualified.

2. **Photography re-foregrounding** — "What if the warm clinician-with-patient photography
   ran at full editorial scale instead of thumbnail teasers?" Evidence: `hero-onkologie`
   2900×1419 + warm human content imagery. Serves variant **C** (editorial/photo bet).
   NOT disqualified — the photography is genuinely strong and on-brand, not generic stock.

3. **Live-data promotion** — "What if find-a-clinic / find-a-doctor search was promoted as
   the primary tool?" Evidence: search-first hero + Ärztesuche + map locator. PARTIAL —
   folds into variant **B**'s need-routing rather than standing alone (no true live
   dashboard data exists).

4. **Signature-gesture extension** — "What if the 18px pill became a consistent system
   motif?" Evidence: 18px pill chips vs 0px squared CTAs/cards (deliberate contrast).
   Serves variant **E** (motif + calm bet). Not disqualified; modest on its own, paired
   with color re-weighting.

5. **Voice-register pivot** — "What if the reassurance voice was given editorial room as
   lead-ins?" Evidence: "Kompetenz, die Vertrauen schafft", "bei allen Folgen des Lebens
   sind wir für Sie da". Folds into variant **C**.

6. **Color-ladder re-weighting** — "What if the warm-neutral ground carried the page and
   blue was used more sparingly but more deliberately as wayfinding?" Evidence: blue
   `#0094d4` rationed already; warm `#f7f6f5`/`#534c46` system. Serves variant **E**
   (calm/neutral-forward bet). Not disqualified.

7. **Audience-routing reframe** — "What if the home was organized around explicit patient
   intent — find a clinic / find a doctor / plan a stay / international / emergency — instead
   of a flat module stack?" Evidence: PRODUCT.md lists parallel JTBD; current IA is a flat
   stack. Serves variant **B** (IA bet). Directly addresses the ia-clutter tension. Not
   disqualified.

8. **Motif vocabulary swap** — "What if the pill/squared contrast was replaced with a single
   consistent shape language?" Folds into variant **E** (motif extension #4) rather than a
   wholesale swap — a wholesale swap would break brand recognition, so kept as extension.

## Disqualified / folded
- #3 Live-data and #8 wholesale motif-swap do not stand as independent variants (no live
  data; wholesale swap breaks recognition). Folded into B and E respectively.

## Variant ↔ candidate map (5 static directions)
- **A** — none (faithful + the 5 improvements above).
- **B** — #7 Audience-routing reframe (+ #3 search-as-tool).
- **C** — #2 Photography re-foregrounding (+ #5 voice-register).
- **D** — #1 Display-typography amplification.
- **E** — #6 Color-ladder re-weighting (+ #4 signature-pill extension).
