<!-- stardust:prototype page-shape brief — home, variant E (Calm / neutral-forward). Static.
     Rendered via impeccable:craft (main-context invocation; subagent spawn was API-throttled). -->

# Page shape — home · Variant E (Calm / neutral-forward)

surprise: high
substrate: single warm ground (`#f7f6f5`) carries the page; `#f0efed` for nested bands; `#ffffff` is the rare lift (search card only). 2 substrate transitions max (warm ↔ warm-alt).

## Amplified moves (the bet)
1. **Color-ladder re-weighting** — warm `#f7f6f5` is the default surface; blue `#0094d4` rationed to true wayfinding actions only (search button, primary CTAs). The captured full-blue "Hirslanden Blog" band is demoted to a warm band with a single blue CTA. Proof numerals and quicklinks drop blue → warm. Emergency red stays the lone saturated accent (144), reading even louder now that blue is rare.
2. **Signature-pill extension** — the captured 18px pill becomes one consistent shape language: every chip, card, tag, input, button, date-chip and the 144 box share `border-radius:18px`. The captured pill-vs-squared contrast is resolved toward the pill.
- Quiet type (Metropolis 500 headings, ratio 1.2, body line-height 1.7) and flat surfaces (shadow only on the one white lift) carry the calm.

## Sections (captured-source lineage)
1. **header** — site-wide system-component (`_brand-extraction.json#systemComponents[kind=header]`); warm ground, hairline, reachable 144 via utility/footer.
2. **hero-search** — `pages/home.json#hero` (search-first wayfinding motif); warm scrim over captured hero photo; H1 weight 500; white search card (the rare lift) + pill chips.
3. **proof-strip** — `pages/home.json` hero subline + footer scale claim (16 / über 300 / über 3000); numerals in warm `#534c46` (rationed blue).
4. **locate** — `pages/home.json#locate` ("Hirslanden in Ihrer Nähe" + "Zu den Standorten").
5. **quicklinks** — `pages/home.json#quicklink-tile-grid`; warm-ground chips, hairline, 18px (not blue-tint).
6. **group-teasers** — `pages/home.json#teaser-card-3up`; flat warm-alt cards, 18px, hairline (no shadow).
7. **whats-happening** — consolidates `news-list` + `event-list-with-date-chip` + `cta-band`; blog band demoted to warm-alt with a blue CTA.
8. **footer** — site-wide system-component (`#systemComponents[kind=footer]`); warm-alt; reserved-red 144.

## Anti-template pass
- cta-band (captured full-blue): default reflex = keep loud blue band; **picked** = warm band + single blue CTA (the rationed-blue amplification). Rationale: E's bet is color re-weighting; a loud blue band contradicts it.
- card grids: default reflex = white shadowed cards; **picked** = flat warm-alt 18px cards (the flat-calm + shape-system moves).

## Voice classification
All copy `captured-verbatim` (German) from `pages/home.json` / `_brand-extraction.json`. Proof numbers = captured fact. No placeholders for prose. Two group-teaser images have no captured asset → flat warm panels with the section label (same honest treatment as the captured site's text teasers), not invented imagery.

## Heading hierarchy
H1 once (hero). Section openers H2. Card/list titles H3. No skips.

## Motion
Static. No choreography, no scroll effects, no cinematic register. Reduced-motion guard present for the one transition block.
